# 📢 在线公告
---

## 🆓 免费声明

**WOA AutoBot 是一款完全开源免费的脚本工具。**

- ✅ 本脚本**永久免费**，不收取任何费用
- ✅ 官方唯一发布渠道：[GitHub](https://github.com/fengfeng-qwq/WOA-AutoBot-GPL3-base)
- ⚠️ **如您是从任何第三方渠道（淘宝、拼多多、微信等）付费购买的，请立即退款并举报！**


---
### v1.6.0-beta1 (2026-09-24) · Beta 测试版

> ⚠️ 从 dev 分支构建，使用正式版 v1.5.2 的用户可忽略。
> 编号由 v1.5.10 进位为 v1.6.0 只是版本号规整，不代表改动变大。

🆕 导出 / 导入配置：高级设置底部一键打包全部实例设置为单个 JSON，换机重装可直接还原
📖 文档写明用户数据目录位置（Windows `%APPDATA%\WOA_AutoBot`）与备份/迁移方法
⚙️ Release 标题去掉「WOA AutoBot」前缀

### v1.5.10-beta2 (2026-09-19) · Beta 测试版

> ⚠️ 本段落属于 **Beta 测试版**（从 dev 分支构建），使用正式版 v1.5.2 的用户可忽略。
> Beta 含较多未经长期运行验证的新功能，不建议用于长时间无人值守挂机。

🆕 航线管理界面自动暂停：检测到你打开航线管理时自动暂停，回到主界面自动恢复（默认关）
🆕 航线页无操作自动返回：停手超过设定时长自动返回主界面继续挂机（默认 5 分钟，0 = 不启用）
🆕 塔台延时档位：自定义每次延时目标时长（1 格 = 10 分钟），0 = 跟随游戏滑条记忆（默认）
🆕 超 2 小时累加续延：设定时长超过游戏单次上限时自动连续续延凑满，只扣 1 次延时次数
🆕 错误频繁自动重启游戏：错误弹窗在设定时间内达到设定次数后自动重启并恢复挂机（默认关）
🆕 跳过未分配航班：需要手动指派的飞机不再被误点（默认开）
🐛 修复游戏更新后塔台菜单打不开的问题
🐛 修复塔台延时可能静默失败（点击被吞、结果误判成功）的问题
🐛 修复高级设置中防检测数值保存后被静默丢弃的问题
🐛 修复分配地勤时按暂停仍会长时间继续分配的问题
⚙️ 启动扫描设备期间显示提示条
📖 离线使用说明全文重写

### v1.5.2 (2026-09-04)

**新功能**
- 🕹️ **新增「离开游戏自动暂停」** — 高级设置 → 设备与方案，通过 `adb shell dumpsys window` 读取 `mCurrentFocus` 判断 `com.haugland.woa` 是否在前台；切出游戏自动暂停，回到游戏自动恢复
- 🌐 **公告改为 GitHub 在线优先** — 在线公告窗口打开时先显示本地内容，后台从 GitHub Raw / jsDelivr 镜像拉取最新版并热替换
- 📱 **小窗模式显示设备地址** — 小窗底部实时显示当前连接设备的 `IP:端口`，启动/停止、扫描设备、切换下拉选择后自动刷新

**修复**
- 🔒 修复周期性误报「已清除 N 个残留锁文件」
- 📡 修复移动端通知因缺失 `urllib.request` 导入而静默失效
- ⚙️ 「离开游戏自动暂停」开关状态在启动/保存时与运行中的 Bot 实例双向同步

### v1.5.1 (2026-08-30)
- 📜 修复高级设置窗口鼠标滚轮无法滚动（Windows 将滚轮事件投递给焦点窗口，此前守卫判断误吞事件）
- 🧵 修复启动时 2~4s 无响应 — 环境检测改为后台线程执行，结果回主线程应用
- 📋 修复日志"消息折叠"提示显示异常的问题

## 最新更新 v1.5.0（2026-08-06）

### 🆕 重大更新

- 🤖 **全面实时识图重构** — 截图缓存引擎（同帧复用 3-5× 加速）、智能状态机（5 状态毫秒级自动识别）、超快速并行任务检测（单帧一次匹配 7 种任务）、主循环空闲间隔 0.12→0.025s（4.8× 提升）
- 📊 **仪表盘全面升级** — 新增下次小退倒计时、塔台延时倒计时、可用地勤实时数、剩余延时次数；塔台状态带倒计时显示（如"全开 4/4 · 5分30秒后延时"）
- 📱 **定时汇报增强** — 手机统计汇报新增实时状态区块（地勤、塔台、小退倒计时）
- 🖥️ **深色/浅色无损切换** — 头部栏、药丸、仪表盘卡片、终端全面实时颜色刷新，切换无延迟无残影

### 🔧 修复与优化

- 🍎 **Aqua Tk 原生剪贴板** — 移除全部 X11 兼容代码（exportSelection=False 等），恢复 macOS Cocoa NSPasteboard 原生 Cmd+C/V
- 🧹 **代码合并精简** — platform_utils→core/platform、woa_debug→core/debug，消除重复定义
- 🧹 **目录整理** — 清理 1.6GB 无用文件（3 个旧 venv、build 临时文件、旧配置、旧崩溃日志）
- ⚡ **流畅度优化** — 自适应空闲间隔（正常 30ms、短缺 60ms）、日志刷屏冷却（8s 去重）、GC 频率降低
- 🐛 **修复 req 变量 UnboundLocalError** — 地勤短缺恢复逻辑重构为安全标志位
- 🐛 **修复 task_fail_cooldown 缺失** — 任务失败后正确加入冷却列表
- 🔒 **跨平台全面验证** — Windows/macOS 双平台兼容性审计通过

### v1.4.2（2026-08-01）

- 🔧 筛选系统升级（4 按钮识图+模板匹配+像素回退）
- 🖥️ 2D/3D 视角自动切换、类别栏扩展至 6 类、塔台二次确认、macOS 兼容

---

## 使用提示

- 🚀 推荐使用 **MuMu 模拟器** 运行游戏
- 🖥️ 建议开启 **2D 模式** 以提升识别准确率
- 📱 **nemu_ipc** 截图方案速度最快，首次启动会自动探测可用方案
- ⚠️ 如遇防卡死误触发，可在设置中调整阈值

---

## 免责声明

本工具仅供学习交流使用，请勿用于商业用途。使用本工具产生的任何后果由用户自行承担。

## 版权声明

- Copyright (C) 2026 nj-yzf（原始项目）
- Copyright (C) 2026 hjtr7mymht-dot（二创修改，源自其 GPL v3 历史版本）
- Copyright (C) 2026 fengfeng-qwq（本仓库修改）
